substitutions:
  _VITE_API_BASE: ""

steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --build-arg
      - VITE_API_BASE=${_VITE_API_BASE}
      - -t
      - ${_IMAGE}
      - .

images:
  - ${_IMAGE}
