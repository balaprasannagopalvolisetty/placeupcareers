substitutions:
  _VITE_API_BASE: "https://placeup-api-rui2a74muq-ue.a.run.app"

steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --build-arg
      - VITE_API_BASE=${_VITE_API_BASE}
      - -t
      - ${_IMAGE}
      - .

images:
  - ${_IMAGE}
